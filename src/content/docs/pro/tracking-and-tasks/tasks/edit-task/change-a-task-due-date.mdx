---
about_tallyfy: true
description: Tallyfy template steps carry relative deadlines like 3 days after launch,
  so a step never holds a calendar date. Real dates appear when you launch a process,
  and from then on you can set any task to an exact date and time, with dependent
  tasks recalculating from their own rules.
id: f314abd16bbafcae390c04f647e862e0
lastUpdated: 2026-09-17
sidebar:
  order: 5
title: Set an exact date on a task
---

import { Steps } from '@astrojs/starlight/components';

:::note[Which app this page describes]
This page describes Tallyfy at `go.tallyfy.com`, the app your team uses today. Screens and
wording can differ in other versions of the Tallyfy interface.
:::

## Why templates hold relative deadlines and tasks hold real dates

A [template](/products/pro/documenting/templates/) is a pattern you reuse, so it can't hold a calendar date. The same onboarding template runs in March and in November, and no single date would be right for both. Template steps carry a rule instead, like "3 days after launch".

Real dates arrive when you [launch a process](/products/pro/launching/how-can-i-launch-a-process-in-tallyfy/). Tallyfy reads every rule, does the arithmetic, and gives each task an actual date on the calendar. From that point on the date belongs to that one [process](/products/pro/tracking-and-tasks/processes/), and you can change it to whatever you want.

So there are three places to set a deadline on a task inside a process, and only the last one takes a date you pick by hand.

### Deadlines in a template are always relative

<Steps>
1. Open your template in edit mode and click the step.
2. On the **Basics** tab, expand the **Timings** row.
3. Set a number and a unit, then choose **after** or **before**, then choose what it counts from: **launch**, or another step in the template.
4. Tallyfy saves the change straight away.
</Steps>

That last dropdown is the whole model. A step is due so many days after the process starts, so many after another step, or so many before a later step. There's no calendar in here, and that's deliberate.

One detail is worth knowing, because it changes partway through a run. A step counting from another step counts from that step's **due date** while the other step is still open, then switches to counting from the time it was **actually completed** once it's done. Full detail is in [how a step deadline is worked out](/products/pro/documenting/templates/edit-templates/step-timings/).

### Adjust a deadline while you launch

The launch screen shows the real date each task will get, and lets you change it for that one run without touching the template.

<Steps>
1. Click **Launch Process** on the template card.
2. Work through the launch screens and pick **Review & edit before launching**.
3. Change the deadline on any task you want to shift.
4. Click **Launch**.
</Steps>

Your template stays exactly as it was. See [edit before launch](/products/pro/launching/how-can-i-edit-a-process-before-launching/) for everything else you can change at this point.

### Set an exact date on a task in a running process

This is the one that takes a real date and time.

<Steps>
1. Open the running process from [Tracker](/products/pro/tracking-and-tasks/tracker-view/).
2. Find the task you want to move.
3. Click the menu icon (...) on the task card and choose **Edit Deadline**.
4. Pick the date and the time you want.
</Steps>

That date belongs to this process only, so the template is untouched and future launches still use its rule.

:::note[The time you pick can still be moved]
Your organization's [work week](/products/pro/settings/org-settings/how-to-adjust-task-deadlines-to-work-week/) is applied to a date you set by hand, the same as any other. Pick a time outside your working hours, or a day that isn't a working day, and Tallyfy moves the deadline forward to the next working time.

Pick 6pm on a Friday in a Monday to Friday, 9 to 5 organization and the task ends up due Monday morning, not Friday evening. If you need the exact time you typed, either set your work week to all hours, or pick a time inside the hours you've configured.
:::

:::note[One-off tasks work the same way]
A [one-off task](/products/pro/tracking-and-tasks/tasks/how-to-create-a-one-off-task-in-tallyfy/) isn't part of a process, so it never had a relative rule to begin with. You give it a date when you create it.
:::

### Tasks that depend on this one move too

If other tasks in the process count their deadline from the one you just changed, each of them is worked out again from its own rule against the new date. A task due 2 days after this one is now due 2 days after the date you picked. That's the point of building the chain in the template: move one date and the rest follow, instead of editing twenty tasks by hand.

Only tasks that aren't finished yet get moved. Your organization's working days apply as well, so a recalculated date that lands on a non-working day gets moved to one that works. See [adjust task deadlines to your work week](/products/pro/settings/org-settings/how-to-adjust-task-deadlines-to-work-week/).

### When the date comes from a form field

Editing a task by hand is fine once. If every run needs its dates to follow a date somebody types in, like a hire date or an event date, don't edit tasks one at a time. Use an automation instead.

Add a **Date** field to your kick-off form, then write a rule such as "set the deadline for Send welcome email to 3 days before Hire Date". It has to be a Date field: Tallyfy won't save a deadline action pointed at a text or number field. Worked examples are in [countdown deadline automation](/products/pro/documenting/templates/automations/examples/countdown-deadline-automation/).

One limit to plan around. An automation runs at most once per process, so if the hire date changes after the automation has already run, the deadlines it set stay where they are and you're back to editing tasks by hand. See [when an automation runs](/products/pro/documenting/templates/automations/when-automations-run/).

import { CardGrid, LinkTitleCard } from "~/components";

## Related articles
<CardGrid>
<LinkTitleCard header="<b>Edit Task > Set an exact date on a task</b>" href="/products/pro/tracking-and-tasks/tasks/edit-task/change-a-task-due-date/" > Tallyfy uses relative deadline rules (like "3 days after launch") in templates since they are... </LinkTitleCard>
<LinkTitleCard header="<b>Edit Templates > How a step deadline is worked out</b>" href="/products/pro/documenting/templates/edit-templates/step-timings/" > Step deadlines in Tallyfy are built from three parts: an anchor (process launch or another step)... </LinkTitleCard>
<LinkTitleCard header="<b>Actions > Automate deadline shifts on tasks</b>" href="/products/pro/documenting/templates/automations/actions/deadline-actions/" > Tallyfy's deadline actions automatically shift task due dates forward or backward when a... </LinkTitleCard>
<LinkTitleCard header="<b>Automations > When an automation runs</b>" href="/products/pro/documenting/templates/automations/when-automations-run/" > Tallyfy automations fire only once per process and are evaluated at just two moments (process... </LinkTitleCard>
</CardGrid>