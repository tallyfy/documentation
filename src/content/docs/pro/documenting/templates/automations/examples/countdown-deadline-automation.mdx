---
about_tallyfy: true
description: Set up automated countdown deadlines that work backwards from important
  dates so preparation tasks are always completed on time.
id: fedc20f2ebac028de31e13ca48377ef4
lastUpdated: 2026-09-17
sidebar:
  order: 9
title: Countdown deadline automation
---

import { Steps } from '@astrojs/starlight/components';
import { Audience } from "~/components";

<Audience to="editor" />

:::note[Which app this page describes]
This page describes Tallyfy at `go.tallyfy.com`, the app your team uses today. Screens and
wording can differ in other versions of the Tallyfy interface.
:::

Tallyfy lets you set [task](/products/pro/tracking-and-tasks/tasks/) deadlines that count backwards from a target date. Your meeting agenda can be automatically due 1 day before the meeting. That's how preparation tasks always finish before the critical date arrives.

:::caution[Read this before you build a countdown around a date that moves]
Each of these automations runs **once** in a given process. It fires, it sets the deadline, and
that's the end of it. If the target date changes afterwards, every deadline it already set stays
exactly where it was.

A countdown is a great fit for a date that's fixed once it's known, like a contract end date. It
needs more care when the date slips, like a rescheduled meeting. See [when an automation
runs](/products/pro/documenting/templates/automations/when-automations-run/) for what to do when
that happens.
:::

## When to use countdown deadlines

- **Meeting prep** - reminders 1 day before scheduled meetings
- **Event planning** - setup tasks due 2 weeks before the event
- **Launch coordination** - marketing materials due 1 week before launch
- **Contract renewals** - start discussions 30 days before expiration
- **Compliance** - submit reports 5 days before regulatory due dates

You're working backwards from a fixed date. Everything lands on time.

```d2
# Countdown deadlines from one target date
direction: right

Room: Book the room {
  shape: rectangle
  style.fill: "#f2faf4"
  style.stroke: "#225930"
}

Research: Research prospect {
  shape: rectangle
  style.fill: "#f2faf4"
  style.stroke: "#225930"
}

Agenda: Send agenda {
  shape: rectangle
  style.fill: "#f2faf4"
  style.stroke: "#225930"
}

Checklist: Day-of checklist {
  shape: rectangle
  style.fill: "#f2faf4"
  style.stroke: "#225930"
}

Target: Meeting date {
  shape: circle
  style.fill: "#e1f7e6"
  style.stroke: "#225930"
  style.stroke-width: 3
}

Room -> Target: 1 week before {
  style.stroke: "#225930"
  style.stroke-width: 2
}
Research -> Target: 3 days before {
  style.stroke: "#225930"
  style.stroke-width: 2
}
Agenda -> Target: 1 day before {
  style.stroke: "#225930"
  style.stroke-width: 2
}
Checklist -> Target: 2 hours before {
  style.stroke: "#225930"
  style.stroke-width: 2
}
```

Each task has no dependency on the others. They are four independent offsets from one date field, which is why they all point at the same anchor.

## How to create countdown deadline automations

<Steps>
1. **Add a date field for the target date**

   In your kick-off form[^1] or an early step, add a **Date** field (e.g. "Meeting date", "Event date", "Contract expiration date"). It has to be a real **Date** field: Tallyfy won't save a deadline action pointed at a text or number field. Even with a Date field, guard against it being blank, because if the value is missing when the automation runs, Tallyfy calculates the deadline from right now instead of leaving it alone, which pushes a countdown deadline into the past.

2. **Add preparation steps**

   Add steps to your [template](/products/pro/documenting/templates/) for the work that needs to happen before that date (e.g. "Send meeting agenda", "Finalize event venue").

3. **Create deadline automations**

   Set up automations that [calculate deadlines](/products/pro/documenting/templates/automations/actions/deadline-actions/) backwards from your date field:

   **Example - meeting reminder 1 day before:**
   ```automation
   IF field "Meeting Date" is not empty
   THEN shift deadline of "Send meeting reminder" by 1 day before field "Meeting Date"
   ```

   **Example - event prep 2 weeks before:**
   ```automation
   IF field "Event Date" is not empty
   THEN shift deadline of "Finalize catering order" by 14 days before field "Event Date"
   ```
</Steps>

## Real-world examples

### Sales funnel

**Kick-off form field**: "Scheduled meeting date"

```automation
IF field "Scheduled Meeting Date" is not empty
THEN shift deadline of "Complete prospect research" by 3 days before field "Scheduled Meeting Date"
```

```automation
IF field "Scheduled Meeting Date" is not empty
THEN shift deadline of "Send meeting agenda" by 1 day before field "Scheduled Meeting Date"
```

```automation
IF field "Scheduled Meeting Date" is not empty
THEN shift deadline of "Meeting day checklist" by 2 hours before field "Scheduled Meeting Date"
```

Valid deadline units are minutes, hours, days, weeks, and months.

### Podcast publishing

**Kick-off form field**: "Podcast launch date"

```automation
IF field "Launch Date" is not empty
THEN shift deadline of "Upload final audio" by 1 week before field "Launch Date"
```

```automation
IF field "Launch Date" is not empty
THEN shift deadline of "Create social media posts" by 3 days before field "Launch Date"
```

```automation
IF field "Launch Date" is not empty
THEN shift deadline of "Final quality check" by 1 day before field "Launch Date"
```

### International shipping

**Step 1 fields**: Location (dropdown) and Expected arrival date (date field)

```automation
IF field "Location" is "International"
THEN show "Verify customs documentation"
```

```automation
IF field "Location" is "International"
AND field "Expected Arrival Date" is not empty
THEN shift deadline of "Verify customs documentation" by 1 week before field "Expected Arrival Date"
```

This automation's condition checks **Location**, not the arrival date, so it doesn't automatically skip a blank date the way the examples above do. Without the second condition, a blank **Expected arrival date** would still let the automation fire, and Tallyfy would calculate the deadline from right now instead of the arrival date, landing it a week in the past and marking the task overdue immediately. Adding `field "Expected Arrival Date" is not empty` stops that: the deadline only shifts once there's a real date to count back from.

International shipments won't get stuck at customs, as long as the arrival date is filled in.

## Advanced patterns

### Multiple deadlines from one date

Create separate automations that all reference the same date field:

```automation
IF field "Contract End Date" is not empty
THEN shift deadline of "Initial renewal discussion" by 60 days before field "Contract End Date"
```
```automation
IF field "Contract End Date" is not empty
THEN shift deadline of "Prepare renewal proposal" by 45 days before field "Contract End Date"
```
```automation
IF field "Contract End Date" is not empty
THEN shift deadline of "Send renewal offer" by 30 days before field "Contract End Date"
```

Each one is a separate automation - they'll all fire independently based on the same date.

### Conditional countdown deadlines

Different lead times based on other field values:

```automation
IF field "Event Type" is "Small Meeting"
AND field "Event Date" is not empty
THEN shift deadline of "Room setup" by 1 hour before field "Event Date"

IF field "Event Type" is "Large Conference"
AND field "Event Date" is not empty
THEN shift deadline of "Room setup" by 1 day before field "Event Date"
```

### Working hours are not applied here

A countdown deadline is set by an automation, and a deadline set by an automation isn't adjusted
to your organization's [work week](/products/pro/settings/org-settings/how-to-adjust-task-deadlines-to-work-week/)[^2].
Tallyfy counts back in calendar time and leaves the answer where it lands, so "14 days before the
event" can land on a Sunday.

Allow for that when you choose the gap. If a task really can't be due at the weekend, pick a gap
that clears it, or set the deadline on the step itself, where the work week is applied.

## Tips

- **Add buffer** - use 2 days before instead of 1 for critical tasks
- **Name tasks clearly** - include timing like "Send agenda (1 day before meeting)"
- **Guard against blank dates** - if the automation's condition doesn't already check the date field, add that check (or make the field required), so a blank date can't push the deadline into the past
- **Set fallback deadlines** - add default deadlines on steps in case date fields are left empty
- **Test around weekends** - verify deadlines calculate correctly near non-working days

:::caution[When a countdown deadline gets set, and why it's only once]
Tallyfy sets a countdown deadline at one of two moments, whichever applies to your automation:

1. When the process launches, if every condition reads a kick-off form field.
2. When somebody completes the step that carries the date field.

It is set **once**. There's no second pass. Reopening the step, editing the date and completing
it again doesn't run the automation a second time, so the deadline it already set doesn't move.

If a date changes after the automation has run, change the affected deadlines by hand. See [when
an automation runs](/products/pro/documenting/templates/automations/when-automations-run/).
:::

import { CardGrid, LinkTitleCard } from "~/components";


[^1]: Pre-launch form that captures initial data before a process starts in Tallyfy
[^2]: Working hours configured in **Settings** > **Organization**, typically Monday-Friday business hours

## Related articles
<CardGrid>
<LinkTitleCard header="<b>Actions > Automate deadline shifts on tasks</b>" href="/products/pro/documenting/templates/automations/actions/deadline-actions/" > Tallyfy's deadline actions automatically shift task due dates forward or backward when a... </LinkTitleCard>
<LinkTitleCard header="<b>Edit Templates > How a step deadline is worked out</b>" href="/products/pro/documenting/templates/edit-templates/step-timings/" > Step deadlines in Tallyfy are built from three parts: an anchor (process launch or another step)... </LinkTitleCard>
<LinkTitleCard header="<b>Automations > When an automation runs</b>" href="/products/pro/documenting/templates/automations/when-automations-run/" > Tallyfy automations fire only once per process and are evaluated at just two moments (process... </LinkTitleCard>
<LinkTitleCard header="<b>Edit Task > Set an exact date on a task</b>" href="/products/pro/tracking-and-tasks/tasks/edit-task/change-a-task-due-date/" > Tallyfy template steps carry relative deadlines like 3 days after launch, so a step never holds... </LinkTitleCard>
</CardGrid>