---
description: Connect to the Tallyfy Manufactory websocket server at wss://collector.tallyfy.com/ws,
  then send an auth message with five required keys to pair the connection with the
  authenticated client before transmitting events.
id: 8e36d835de5e733708adecf532b21672
lastUpdated: 2026-03-22
pcx_content_type: connect_to_ws
sidebar:
  order: 2
title: Connect to the websocket server
---

After successful authentication and getting client UUID along with actor profile ID[^1], you can connect to the Tallyfy Manufactory websocket server: `wss://collector.tallyfy.com/ws`

Once a websocket connection is established, you'll receive a message `Manufactory Collector ✅`

## WebSocket authentication flow

This diagram shows the authentication handshake between your client and the Manufactory WebSocket server.

```d2
# WebSocket Authentication Flow
direction: down

# Actors
Client: Client Application {
  shape: person
  style.fill: "#e1f7e6"
}

WebSocket: WebSocket Server {
  shape: cloud
  style.fill: "#f2faf4"
}

# Connection Phase
Client -> WebSocket: 1. Connect to WSS {
  style.stroke: "#225930"
  style.stroke-width: 2
}

WebSocket -> Client: 2. Connection established {
  style.stroke: "#225930"
  style.stroke-dash: 3
}

# Authentication Phase
Client -> WebSocket: 3. Send auth message {
  style.stroke: "#225930"
  style.stroke-width: 3
}

WebSocket -> WebSocket: 4. Validate credentials {
  style.stroke: "#225930"
}

WebSocket -> Client: 5. Auth confirmation {
  style.stroke: "#225930"
  style.stroke-dash: 3
}

# Ready State
Ready: Ready to Send Events {
  shape: circle
  style.fill: "#e1f7e6"
  style.stroke: "#225930"
  style.stroke-width: 2
}

WebSocket -> Ready: 6. Auth complete {
  style.stroke: "#225930"
  style.stroke-width: 2
}

Client -> Ready: 7. Begin sending events {
  style.stroke: "#225930"
  style.stroke-width: 2
}
```

### What to notice

- **Two-phase authentication**: First the WebSocket connection itself, then the authentication message exchange
- **Required credentials**: The auth message must include all five required keys (type, org_id, project_id, client_uuid, actor_profile_id)
- **Sequential flow**: Each step must complete successfully before proceeding. The connection won't accept events until authentication succeeds

Next, we need to pair the connection with the client/end-user. That's where the actor profile ID and client UUID become active.

### Required keys

- `type` (required) string - The message type, in this case we will be using `auth`.
- `org_id` (required) string - Manufactory org ID.
- `project_id` (required) number - The project ID associated with this schema.
- `actor_profile_id` (required) number - The actor profile ID we got after authentication.
- `client_uuid` (required) string - the generated UUID we got after authentication.
### Websocket message example
```json
{
  "type": "auth",
  "org_id": "xyz",
  "project_id": 12345,
  "client_uuid": "e17a56a9-596f-40e6-b01c-d37136df1de8",
  "actor_profile_id": 1
}
```

### Websocket reply example

```json
{
  "type": "auth",
  "status": "ok"
}
```

Now you can send the actor's generated events through the open websocket channel.

import { CardGrid, LinkTitleCard } from "~/components";


[^1]: A unique identifier for the authenticated user within the Tallyfy organization context

## Related articles
<CardGrid>
<LinkTitleCard header="<b>Websocket > Websocket collector authentication</b>" href="/products/manufactory/collector/websocket/auth/" > Authenticate websocket clients to Tallyfy Manufactory via the POST /auth endpoint. You'll need... </LinkTitleCard>
<LinkTitleCard header="<b>Websocket > Send events via websocket</b>" href="/products/manufactory/collector/websocket/send_events/" > Send events to Tallyfy Manufactory through a websocket using a JSON message with a type field... </LinkTitleCard>
<LinkTitleCard header="<b>Collector > Collecting events via websocket</b>" href="/products/manufactory/collector/websocket/" > Tallyfy Manufactory's websocket connection provides real-time two-way communication between... </LinkTitleCard>
<LinkTitleCard header="<b>Collector > Collecting events via the REST API</b>" href="/products/manufactory/collector/rest_api/" > Send actors data and events into Tallyfy Manufactory using the REST API. You can also use... </LinkTitleCard>
</CardGrid>