---
id: ad6f7edbe827ecd8b7e21a1ef97a4649
pcx_content_type: create_schema
sidebar:
  order: 2
snippet: The POST /events/schema endpoint enables creation of event schemas by defining
  organization ID project ID and attribute specifications while requiring authentication
  through a Manufactory access token.
title: Create events schema
---

## Design your events schema

You can create events schema using the endpoint `POST /events/schema`.

## Authentication

The following header is required for all API calls:
- `X-Manufactory-Auth` - Manufactory access token

### Required keys

- `org_id` (required) string - Manufactory org ID.
- `project_id` (required) number - The project ID associated with this schema.
- `attributes` (required) object - Pairs containing attribute name and the corresponding data type. A complete list of [valid data types](/products/manufactory/data-types/).

### Request example
```json
{
  "org_id": "xyz",
  "project_id": 12345,
  "attributes":{
    "Event name": "TEXT",
    "Event type": "TEXT",
    "Made purchase": "BOOLEAN"
  }
}
```

### Response example

```json
{
  "message": "Events schema created successfully"
}
```

import { CardGrid, LinkTitleCard} from "~/components";

## Related articles
<CardGrid>
<LinkTitleCard header="<b>Events > Get events schema</b>" href="/products/manufactory/events/get_schema/" > The events schema endpoint allows retrieval of project-specific event attribute definitions through a GET request using organization ID and project ID, returning details like event names, types, and data formats. </LinkTitleCard>
<LinkTitleCard header="<b>Actors > Create actors schema</b>" href="/products/manufactory/actors/create_schema/" > The POST /actors/schema endpoint allows creation of actor schemas by specifying organization ID, project ID, attributes with data types, and a unique identifier field while requiring authentication via a Manufactory access token. </LinkTitleCard>
<LinkTitleCard header="<b>Actors > Get actors schema</b>" href="/products/manufactory/actors/get_schema/" > The actors schema endpoint returns attribute details like city, country, email, and username for a specified organization and project when authenticated with a Manufactory access token. </LinkTitleCard>
<LinkTitleCard header="<b>Events > Delete events schema</b>" href="/products/manufactory/events/delete_schema/" > Event schemas can be removed by sending a DELETE request with authentication via a Manufactory access token while specifying the organization and project IDs in the endpoint path. </LinkTitleCard>
</CardGrid>