# D2 palette baseline - tallyfy/documentation#192
#
# Diagrams that already used off-palette colours when the palette check
# was introduced. One '<path>\t<fence number>' per line.
#
# This is a RATCHET, not an allowance. A NEW off-palette diagram fails
# the gate. An entry here whose diagram has since been fixed ALSO fails
# the gate, telling you to delete the line, so the list can only shrink.
#
# See the whole list with: scripts/validate-d2.sh --strict
# Regenerate with:         scripts/validate-d2.sh --write-baseline
